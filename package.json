{
  "name": "sorts-implements-in-js",
  "version": "0.0.1",
  "description": "sorts implements in js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "sorts",
    "algotithm"
  ],
  "author": "forclan",
  "license": "ISC",
  "dependencies": {
    "mocha": "^2.5.3"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.9.0"
  }
}
